<% include ../header.ejs %>
	
	<div class="row">
		<div class="span12">
			<h1>Tournament Details</h1>
		</div>
	</div>
	
	<% if (tournament === null ) { %>
	
	<div class="row shift-down">
		<div class="span12">
			<div class="alert alert-error">Unable to find tournament</div>
		</div>
	</div>
	
	<% } else { %>
	<div class="row shift-down">
        <div class="span6">
            <h3><%= tournament.name %></h3>
            <p>From <%= tournament.start_date %> <br/>to <%= tournament.end_date %></p>
        </div>
        
        <div class="span6">
            <h3>Teams Involved</h3>
            <a href="/error">Invite a Team</a>
            <% if (typeof tournament.teams === 'undefined') { %>
            <div class="row">
                <div class="span5">
                    <div class="alert alert-warning">No teams are registered</div>
                </div>
            </div>
            <% } else { %>
                <% for(var i = 0; i < tournament.teams.length; i++) { %>
                    s
                <% } %>
            <% } %>
        </div>    
    <% } %>
    
<% include ../footer.ejs %>
